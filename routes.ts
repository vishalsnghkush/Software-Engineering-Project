export const publicRoutes = ["/", "/api/init"];

export const authRoutes = ["/login", "/signup"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGGEDUSER_REDIRECT = "/home";

export const DEFAULT_GUEST_REDIRECT = "/login";
