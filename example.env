POSTGRES_USER='se_admin' 
POSTGRES_PASSWORD='password' # <-- Changing the default value is recommended 
POSTGRES_DB='project_se'
POSTGRES_PORT='5433'

PGADMIN_DEFAULT_EMAIL='admin@email.com'
PGADMIN_DEFAULT_PASSWORD='admin@123' # <-- Changing the default value is recommended


# TODO: Set default port for production
DATABASE_URL="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}"

BETTER_AUTH_SECRET="secret" # <-- Changing the default value to a valid secret is necessary
BETTER_AUTH_URL="http://localhost:3000"